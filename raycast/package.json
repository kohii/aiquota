{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "aiquota",
  "title": "AI Usage",
  "description": "Show claude / codex / cursor / copilot subscription usage at a glance, with colored progress and reset times.",
  "icon": "command-icon.png",
  "author": "kohii",
  "license": "MIT",
  "commands": [
    {
      "name": "ai-usage",
      "title": "AI Usage",
      "subtitle": "aiquota",
      "description": "Show claude / codex / cursor / copilot subscription usage.",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "aiquotaPath",
      "title": "aiquota Binary",
      "description": "Absolute path to the aiquota binary. Leave empty to auto-detect (~/go/bin, Homebrew, /usr/local/bin, ~/.local/bin, PATH).",
      "type": "textfield",
      "required": false,
      "default": "",
      "placeholder": "/Users/you/go/bin/aiquota"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.85.0",
    "@raycast/utils": "^1.18.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.11",
    "@types/node": "^20.14.0",
    "@types/react": "19.0.10",
    "eslint": "^8.57.0",
    "prettier": "^3.3.3",
    "typescript": "^5.5.4",
    "vitest": "^2.1.8"
  },
  "packageManager": "pnpm@11.5.0",
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "lint": "ray lint",
    "fix-lint": "ray lint --fix",
    "test": "vitest run"
  }
}
